{
  "scripts": {
    "lint": "eslint \"js/**/*.js\"",
    "security-patterns": "node scripts/security-scan.mjs",
    "security-check": "audit-ci --moderate",
    "prebuild": "npm run lint && npm run security-patterns && npm run security-check",
    "prepare": "husky"
  },
  "devDependencies": {
    "audit-ci": "^7.1.0",
    "eslint": "^8.57.1",
    "eslint-plugin-security": "^4.0.0",
    "husky": "^9.1.7",
    "purgecss": "^8.0.0",
    "sharp": "^0.34.5"
  }
}
